#include "WPILib.h"
int convertedDist;

class Robot: public IterativeRobot
{
private:
	LiveWindow *lw = LiveWindow::GetInstance();
	AnalogInput *distanceSensor;

	void RobotInit()
	{
		distanceSensor = new AnalogInput(3);
	}

	void AutonomousInit()
	{

	}

	void AutonomousPeriodic()
	{

	}

	void TeleopInit()
	{

	}

	void TeleopPeriodic()
	{
		ReadDistance();
	}

	void ReadDistance() {
		SmartDashboard::PutNumber("the raw distance from the distance sensor is", distanceSensor->GetValue());
		convertedDist = 6 * distanceSensor->GetValue() / 54;
		SmartDashboard::PutNumber("the converted distance is", convertedDist);
	}

	void TestPeriodic()
	{
		lw->Run();
	}
};

START_ROBOT_CLASS(Robot)
